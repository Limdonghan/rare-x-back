version: '3.8'

services:
  # Redis
  redis:
    image: redis:latest
    container_name: rare-x-redis
    ports:
      - "6379:6379"
    restart: always
    networks:
      - rare-x-network

networks:
  rare-x-network:
    driver: bridge
